# Fighter sprites

This directory is populated by `npm run assets:vendor`.

`marcelo/` is supplied locally by the project owner and is included directly in builds; it does not depend on the vendor download command.

The generated PNG files are intentionally not fabricated when network access is unavailable. Their provenance and licenses are documented in `ASSETS.md`.
